version: 1

# SILVER
models:
  - name: s_material
    columns:
      - name: material_id
        tags:
          - primary-key
        tests:
         - unique
         - not_null
    meta:
      datafold:
        datadiff:
          never_diff: true
          include_columns:
            - material_id
            - uom_base
            - material_group
            - description

  - name: s_sales_order_header
    columns:
      - name: document_id
        tags:
          - primary-key
    meta:
      datafold:
        datadiff:
          never_diff: true

  - name: s_sales_order_line
    columns:
      - name: document_id
        tags:
          - primary-key
      - name: item_id
        tags:
          - primary-key
    meta:
      datafold:
        datadiff:
          never_diff: true