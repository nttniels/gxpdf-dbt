version: 1

# SILVER
models:
  - name: s_material
    columns:
        - name: material_id
          tags:
            - primary-key

  - name: s_sales_order_header
    columns:
        - name: document_id
          tags:
            - primary-key

  - name: s_sales_order_line
    columns:
          - name: document_id
            tags:
              - primary-key
          - name: item_id
            tags:
              - primary-key
        
